<h1>Vote</h1>
<%= simple_form_for @boto do |f| %>
  <%= f.association :candidate, :label_method => :first_name, as: :select, :collection => Candidate.all %>
  <%= f.input :comments, label: "Comments" %>
  <%= f.button :submit %>
<% end %>
