<h1>Vote</h1>
<%= simple_form_for @vote do |f| %>
  <%= f.association :name, as: :select, label_method: :candidates, collection: candidates.all %>
  <%= f.input :comments, label: "Comments" %>
  <%= f.button :submit %>
<% end %>
